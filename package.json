{
  "name": "hello_subbot",
  "version": "1.0.0",
  "description": "A Telegram bot for querying subscription traffic information using Cloudflare Workers",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "wrangler dev --local",
    "setup": "powershell -ExecutionPolicy Bypass -File deploy.ps1 -Install"
  },
  "keywords": [
    "telegram",
    "bot",
    "subscription",
    "cloudflare",
    "workers",
    "vpn",
    "traffic"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
